# 🚀 CONFIGURACIÓN NGINX - CORNER BOOKS LIBRARY SYSTEM
# =================================================================

# 📋 1. CONFIGURACIÓN PRINCIPAL
# Usuario que ejecuta nginx (seguridad - sin privilegios)
user nobody;

# Procesos automáticos basados en CPU (8 cores = 8 workers)
# Resultado: 8 workers × 1024 conexiones = 8,192 usuarios simultáneos
worker_processes auto;

# Archivo donde se guardan todos los errores para debugging
error_log /opt/homebrew/var/log/nginx/error.log;

# Archivo que contiene el ID del proceso principal de nginx
pid /opt/homebrew/var/run/nginx.pid;

# ⚡ 2. BLOQUE EVENTS - Configuración de conexiones
events {
    # Cada worker puede manejar 1024 conexiones simultáneas
    # Total: 8 workers × 1024 = 8,192 conexiones simultáneas
    worker_connections 1024;
}

# 🌐 3. BLOQUE HTTP - Configuración principal del servidor web
http {
    # Incluye tipos MIME (jpg, css, js, html, etc.)
    include       mime.types;
    
    # Tipo por defecto si nginx no reconoce el archivo
    default_type  application/octet-stream;
    
    # 📊 LOGS - Registro de todas las requests
    # Log de accesos: GET, POST, status codes, IPs, etc.
    access_log /opt/homebrew/var/log/nginx/access.log;
    
    # 🚀 OPTIMIZACIONES DE PERFORMANCE
    # Envía archivos directamente desde kernel (ultra rápido)
    sendfile on;
    
    # Agrupa datos antes de enviar (optimización de red)
    tcp_nopush on;
    
    # Envía datos inmediatamente cuando están listos
    tcp_nodelay on;
    
    # Mantiene conexiones abiertas 65 segundos (reutilización)
    keepalive_timeout 65;
    
    # Tamaño de tabla hash para tipos MIME
    types_hash_max_size 2048;

    # 🗜️ COMPRESIÓN GZIP - Reduce tamaño de archivos 70%
    # Activa compresión automática
    gzip on;
    
    # Añade header "Vary: Accept-Encoding" para proxies
    gzip_vary on;
    
    # Solo comprime archivos mayores a 1KB (eficiencia)
    gzip_min_length 1024;
    
    # Tipos de archivo que se comprimen (texto, JavaScript, JSON, etc.)
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # 🏢 SERVIDOR PRINCIPAL - Configuración del host virtual
    server {
        # Escucha en puerto 80 (HTTP estándar - no necesita escribirse en URL)
        listen 80;
        
        # Responde solo a requests de "localhost"
        server_name localhost;

        # 🔗 LOCATION /API/ - Proxy para Backend Node.js
        # Cualquier URL que empiece con /api/ se envía al backend
        # Ejemplo: localhost:80/api/books → localhost:5000/api/books
        location /api/ {
            # Envía todas las requests de API al backend Node.js
            proxy_pass http://localhost:5000;
            
            # Usa HTTP/1.1 (compatible con features modernas)
            proxy_http_version 1.1;
            
            # Headers para WebSockets (upgrade de HTTP → WebSocket)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            
            # Mantiene el host original (localhost:80)
            proxy_set_header Host $host;
            
            # IP real del usuario (para logs y analytics)
            proxy_set_header X-Real-IP $remote_addr;
            
            # Cadena de proxies (si hay varios intermediarios)
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            
            # Protocolo original (HTTP o HTTPS)
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # No hacer cache de WebSockets
            proxy_cache_bypass $http_upgrade;
        }

        # ⚡ CONFIGURACIÓN ESPECIAL PARA VITE - Hot Module Replacement
        # Archivos especiales de Vite para desarrollo
        # Ejemplo: localhost:80/@vite/client → localhost:5173/@vite/client
        location /@vite/ {
            # Envía a Vite dev server
            proxy_pass http://localhost:5173;
            proxy_http_version 1.1;
            
            # Headers para WebSocket (Hot Module Replacement)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            
            # No cache para desarrollo
            proxy_cache_bypass $http_upgrade;
        }

        # 📁 ASSETS DE DESARROLLO - Código fuente de React
        # Módulos fuente de tu aplicación React
        # Ejemplo: localhost:80/src/components/Header.jsx → localhost:5173/src/components/Header.jsx
        location /src/ {
            # Envía a Vite para compilación
            proxy_pass http://localhost:5173;
            proxy_http_version 1.1;
            
            # Headers para hot reload
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            
            # No cache para desarrollo
            proxy_cache_bypass $http_upgrade;
        }

        # 🖼️ ARCHIVOS ESTÁTICOS - JavaScript, CSS, imágenes, fuentes
        # Expresión regular para archivos estáticos comunes
        # ~* = case-insensitive regex
        # \. = punto literal
        # $ = final de string
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
            # Envía a Vite (que los sirve optimizados)
            proxy_pass http://localhost:5173;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            
            # 🎯 CACHE AGRESIVO - Archivos estáticos no cambian
            # Browser guarda archivo por 1 año
            expires 1y;
            
            # Header que indica "nunca cambia, cache forever"
            add_header Cache-Control "public, immutable";
        }

        # 🔌 WEBSOCKETS - Para Hot Module Replacement de Vite
        # Comunicación en tiempo real entre browser y Vite
        # Para que cambios en código se vean instantáneamente
        location /ws {
            proxy_pass http://localhost:5173;
            proxy_http_version 1.1;
            
            # Headers específicos para upgrade HTTP → WebSocket
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
        }

        # 🏠 CATCH-ALL FRONTEND - React Router y páginas principales
        # REGLA FINAL: Todo lo que NO coincida con rules anteriores
        # Ejemplos que llegan aquí:
        # - localhost:80/ (página principal)
        # - localhost:80/catalog (React Router)
        # - localhost:80/book/123 (React Router)
        # - localhost:80/search (React Router)
        location / {
            # Envía todo al frontend React/Vite
            proxy_pass http://localhost:5173;
            proxy_http_version 1.1;
            
            # Headers para SPA (Single Page Application)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            
            # No cache para desarrollo
            proxy_cache_bypass $http_upgrade;
        }
    }
}

# 📊 ORDEN DE EVALUACIÓN DE LOCATIONS:
# nginx evalúa locations en este orden de prioridad:
# 1. location /api/          ← MÁS ESPECÍFICO (exact match)
# 2. location /@vite/        ← MÁS ESPECÍFICO (exact match)  
# 3. location /src/          ← MÁS ESPECÍFICO (exact match)
# 4. location ~* \.(js|css)  ← REGEX (expresiones regulares)
# 5. location /ws            ← ESPECÍFICO (exact match)
# 6. location /              ← CATCH-ALL (último, menos específico)

# 🎯 FLUJO DE EJEMPLO:
# Usuario → localhost:80/api/books
#   ↓ nginx evalúa
#   ↓ ¿Empieza con /api/? → SÍ
#   ↓ proxy_pass → localhost:5000/api/books
#   ↓ Node.js consulta MongoDB
#   ↓ Respuesta: JSON con 29 libros
#   ↓ nginx retorna a usuario

# 🚀 BENEFICIOS DE ESTA CONFIGURACIÓN:
# ✅ Un solo puerto (80) para todo
# ✅ Hot Module Replacement funciona
# ✅ APIs sin problemas CORS
# ✅ Cache optimizado para performance
# ✅ 8,000+ usuarios simultáneos
# ✅ Compresión automática (70% menos datos)
# ✅ Logs completos para debugging
# ✅ Arquitectura de nivel enterprise 